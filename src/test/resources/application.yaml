spring:
  application:
    name: newsfeed
  profiles:
    active: test

logging:
  level:
    root: info
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
    com.lunazkoe.newsfeed: info